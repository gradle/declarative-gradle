package com.example

// This fails when uncommented (for non-demoRelease compilations) since the dependency
// is only added to the demoRelease target

class Example2 {
    init {
//        retrofit2.Retrofit.Builder()
    }
}